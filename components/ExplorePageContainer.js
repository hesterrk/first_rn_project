import React from "react";
import {
    Text,
    SafeAreaView
  } from "react-native";

export default function ExplorePageContainer({ navigation }) {
  return (
    <SafeAreaView>
        <Text>Explore</Text>
    </SafeAreaView>
  );
}

